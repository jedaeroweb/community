<%
    a_class = 'nav-link'
    b_class = 'nav-link'

    if params[:controller] == 'admin/talks'
      a_class += ' active'
    end

    if params[:controller] == 'admin/notices'
      b_class += ' active'
    end


  %>
  <div class="col-12">
    <nav class="sub-nav">
      <ul class="nav nav-pills">
        <li class="nav-item"><%= link_to t(:talk, scope: [:activerecord, :models]), admin_talks_path, { :class => a_class } %></li>
        <li class="nav-item"><%= link_to t(:notice, scope: [:activerecord, :models]), admin_notices_path, { :class => b_class } %></li>
      </ul>
    </nav>
  </div>