<%= form_with(model: [:admin, material], local: true) do |form| %>
  <% if material.errors.any? %>
    <div id="error_explanation" class="alert alert-danger">
      <a href="#" class="close" data-dismiss="alert">&times;</a>
      <h2><%= pluralize(material.errors.count, "error") %> prohibited this request from being saved:</h2>
      <ul>
        <% material.errors.full_messages.each do |msg| %>
          <li>
            <%= msg %>
          </li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div class="card">
    <div class="card-body">
      <div class="form-group">
        <%= form.label :title %>
        <%= form.text_field :title, :class => 'form-control', :required => 'required' %>
      </div>
      <div class="form-group">
        <%= form.label :price %>
        <%= form.number_field :price, :class => 'form-control', :required => 'required' %>
      </div>
      <div class="form-group">
          <%= form.label :picture %>
          <%= form.file_field :picture, :class => "form-control-file" %>
      </div>
    </div>
  </div>
  <div class="form-group actions">
    <%= form.submit :class => "btn btn-primary btn-block btn-lg" %>
  </div>
<% end %>